/**
 * AIX Unified Storage Adapter — Studio re-export
 * Re-exports from @aix-core package to avoid duplication.
 */
export { kv, NS, TTL, KEYS, type StorageOptions, type StorageAdapter } from '@aix-core';
