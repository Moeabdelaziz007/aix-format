{
  "name": "@axiom/health",
  "version": "0.1.0",
  "description": "Repo health score: dead-code, coverage threshold, trust-chain integrity, type-drift. Replaces dead-code-scan.sh + iqra:grow + health-score.ts.",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "axiom-health": "bin/axiom-health.js"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "files": [
    "src",
    "bin",
    "README.md"
  ]
}
