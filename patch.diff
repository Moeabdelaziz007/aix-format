--- .git/hooks/sendemail-validate.sample
+++ .git/hooks/sendemail-validate.sample
@@ -32,8 +32,15 @@
	file="$1"
	# Ensure that the patch applies without conflicts.
	git am -3 "$file" || return
-	# TODO: Replace with appropriate checks for this patch
-	# (e.g. checkpatch.pl).
+
+	# Run appropriate checks for this patch.
+	# Install dependencies if necessary, then run tests and validation.
+	if [ -f package.json ]; then
+		echo "Running project tests and validation for the patch..."
+		npm ci --silent || npm install --silent
+		npm test || return 1
+		npm run validate:examples || return 1
+	fi
	true
 }
