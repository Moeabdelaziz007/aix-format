# deploy-studio.yml is now consolidated into ci.yml
# This file is kept as a no-op to avoid breaking any references.
name: '[DEPRECATED] Deploy Studio'

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Deployments are handled by ci.yml"
