name: 🧬 AIX Sovereign Evolution

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  evolve:
    name: 🚀 Autonomous Self-Improvement
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Sovereign Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: 📦 Install Dependencies
        run: pnpm install

      - name: 🧬 Activate Meta-Loop
        run: npx tsx scripts/activate-meta-loop.ts --infinite
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          UPSTASH_REDIS_REST_URL: ${{ secrets.UPSTASH_REDIS_REST_URL }}
          UPSTASH_REDIS_REST_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
          NOMIC_API_KEY: ${{ secrets.NOMIC_API_KEY }}
          LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
          LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
          LANGFUSE_HOST: "https://cloud.langfuse.com"

      - name: 🏥 Self-Heal & Report
        if: always()
        run: pnpm run health:check
        
      - name: 📬 Commit Lessons
        if: always()
        run: |
          git config --local user.email "aix-agent@amrikyy.ai"
          git config --local user.name "AIX Sovereign Agent"
          git add .generated/HEALTH_REPORT.md
          git commit -m "chore(evolution): graft new wisdom [skip ci]" || echo "No new lessons"
          git push
          echo "Lessons recorded"
